#This file tells the application where the database is located so the application knows where to ask for the data.

#Database connection string (this is where the database is located)
database_url = "postgresql://user:password@localhost/db"
